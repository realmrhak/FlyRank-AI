{
  "name": "ai-decision-flow-backend",
  "version": "1.0.0",
  "description": "Backend for the AI Decision Flow assignment — runs workflow steps via Inngest and calls the AI for each node.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "nodemon --env-file=.env src/index.js",
    "inngest:dev": "npx inngest-cli@latest dev"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "inngest": "^3.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
